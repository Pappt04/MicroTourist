# Copy this file to .env and fill in the values:
#   cp .env.example .env

# PostgreSQL credentials
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=microtourist-purchase

# Token signing secret (shared with stakeholders service)
TOKEN_SECRET=

# Port the service is exposed on the host
PORT=8082
