# Copy to .env and fill in values:
#   cp .env.example .env

# MongoDB connection URI
MONGO_URI=mongodb://localhost:27017

# MongoDB database name
MONGO_DB=microtourist_blog

# Port the service is exposed on the host
PORT=8081
